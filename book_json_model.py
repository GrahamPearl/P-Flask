import json

book_fields = [ 
  "id", 
  "name",
  "price",
  "email"
]
      
def convertSQLtoJSON(data):
    print(data)
    #names = list(map(lambda x: x[0], data.description))    
    #print(names)
            
    #json_object = json.dumps(dictObj) 
    #print(json_object)
    return True